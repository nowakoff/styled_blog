<div id="page_wrapper">
  <h1>New Post</h1>
  <%= form_for :post, url: posts_path do |f| %>
    <% if @errors %>
      <h2><%= pluralize(@post.errors.count, "error")%>  prevented this post form saving</h2>
      <ul>
        <% @errors.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    <% end %>
    <p><%= f.label :title %><br>
      <%= f.text_field :title %>
    </p>
      <p><%= f.label :body %><br>
      <%= f.text_area :body %>
    </p>
    <p><%= f.submit   %>
    </p>
  <%end%>
</div>
